#include <stdio.h>

int main(){
 float ar, vol, ra, pi = 3.14159;

  printf("Digite o valor do raio:");
  scanf("%f", &ra);

  ar = 4 * pi * ra * ra;
  vol = (4/3.0) * pi * ra * ra * ra;

  printf("A area da superficie é:%.2f\n", ar);
  printf("O volume é:%.2f\n", vol);
  
  return 0;
}
